name: Morning Pulse — 7am Pre-market
on:
  schedule:
    - cron: '0 20 * * 0-4'  # 8pm UTC = 7am AEST (adjust for AEDT)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  briefing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python -m pipeline.main --briefing-type 7am
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          DASHBOARD_URL: ${{ secrets.DASHBOARD_URL }}
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update 7am briefing data"
          file_pattern: "data/*.json frontend/public/data/*.json"
